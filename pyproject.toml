[project]
name = "worksync-mcp"
version = "0.1.0"
description = "WorkSync MCP server for multi-agent work tracking coordination"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.26.0",
    "pyyaml>=6.0",
]
